name: Deploy to Cloudflare Worker

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install dependencies
        run: npm install

      - name: Build FX Client
        run: npm run build

      - name: Create Index Redirect
        run: echo '<meta http-equiv="refresh" content="0; url=latest.html">' > index.html

      # NEW STEP: Delete Node files so Cloudflare serves only static files
      - name: Clean up Node files
        run: |
          rm -rf node_modules
          rm -f package.json
          rm -f package-lock.json
          rm -f index.js

      - name: Deploy to Cloudflare Worker
        uses: cloudflare/wrangler-action@v3
        with:
          apiToken: ${{ secrets.CF_API_TOKEN }}
          accountId: ${{ secrets.CF_ACCOUNT_ID }}
          wranglerVersion: 'latest'
          command: deploy
